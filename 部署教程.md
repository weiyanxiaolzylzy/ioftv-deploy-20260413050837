# IOFTV 服务器部署教程

这份文档是当前项目的唯一有效部署说明，目标是让接手人只靠命令行就能完成部署、排障和回滚。

当前交付部署路径：

- `Xshell`
- `Nginx`
- `PM2`
- `PostgreSQL`
- 不使用宝塔面板
- 不使用 Docker 作为当前默认交付方式

## 1. 部署目标

服务器最终部署目录固定为：

```text
/www/wwwroot/ioftv
```

后端服务固定监听：

```text
127.0.0.1:8890
```

Nginx 负责：

- 托管前端静态资源
- 托管 IFC 查看器静态资源
- 转发 `/api/` 和 `/bigscreen/` 到后端

当前常用访问地址：

- 局域网：`http://192.168.60.10`
- Tailscale：`http://100.104.212.6`

## 2. 服务器运行结构

最终服务器结构应当至少包含：

```text
/www/wwwroot/ioftv/
├── dist/                          主前端构建产物
├── ifc/
│   └── dist/                      IFC 查看器构建产物
├── server/
│   ├── index.js
│   ├── package.json
│   ├── package-lock.json
│   ├── uploads/
│   │   ├── ifc/
│   │   └── ifc-cache/
│   └── ...
├── public/
│   └── static/js/wasm/
├── banzu/
├── storage/
├── 单个构件质检表/
├── server-permission-baseline.sh
└── server-reset-clean.sh
```

## 3. Nginx 路由关系

当前有效路由关系应为：

- `/` -> `/www/wwwroot/ioftv/dist`
- `/ifc/` -> `/www/wwwroot/ioftv/ifc/dist`
- `/uploads/` -> `/www/wwwroot/ioftv/server/uploads`
- `/banzu/` -> `/www/wwwroot/ioftv/banzu`
- `/public/` -> `/www/wwwroot/ioftv/public`
- `/wasm/` -> `/www/wwwroot/ioftv/public/static/js/wasm`
- `/api/` -> `http://127.0.0.1:8890`
- `/bigscreen/` -> `http://127.0.0.1:8890`

注意：

1. `/ifc/` 是查看器页面，不是 IFC 文件目录
2. IFC 上传文件真实目录是 `server/uploads/ifc`
3. `try_files` 配错时，静态 JS 很容易错误回退成 `index.html`

## 4. 服务器预备环境

登录服务器后先检查：

```bash
nginx -v
node -v
npm -v
pm2 -v
psql --version
git --version
unzip -v
```

如缺失：

```bash
npm install -g pm2
```

检查 PostgreSQL：

```bash
systemctl status postgresql
ss -lntp | grep 5432
```

如未启动：

```bash
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

## 5. 本地构建

### 5.1 构建主前端

```bash
npm install
npm run build
```

### 5.2 构建 IFC 查看器

```bash
cd ifc
npm install
npm run build
cd ..
```

构建完成后必须确认：

```bash
dir dist
dir ifc\dist
```

缺少任一目录都会导致服务器前端资源不完整。

## 6. 上传到服务器的内容

至少需要上传：

- `dist/`
- `ifc/dist/`
- `server/`
- `public/`
- `banzu/`
- `单个构件质检表/`
- `package.json`
- `package-lock.json`
- `server-permission-baseline.sh`
- `server-reset-clean.sh`
- `README.md`
- `部署教程.md`

推荐做法是本地打包后上传压缩包，服务器解压覆盖。

服务器端示例：

```bash
mkdir -p /www/wwwroot/ioftv
cd /www/wwwroot/ioftv
unzip -o ioftv.zip
```

## 7. 服务器目录初始化

第一次部署或清空后，先执行：

```bash
mkdir -p /www/wwwroot/ioftv
mkdir -p /www/wwwroot/ioftv/server/uploads/ifc
mkdir -p /www/wwwroot/ioftv/server/uploads/ifc-cache
mkdir -p /www/wwwroot/ioftv/public/static/js/wasm
mkdir -p /www/wwwroot/ioftv/banzu
mkdir -p /www/wwwroot/ioftv/storage
```

## 8. 权限基线

这是当前部署里最关键的一步。

### 8.1 为什么必须做

这个项目多次出现“权限化石”问题，即：

- `root` 曾经跑过 PM2
- `www-data` 曾经拥有过上传目录
- 实际 Node 进程又是 `dell`

会直接引发：

- `EACCES: permission denied, open /server/uploads/ifc/...`
- `EACCES: permission denied, mkdir /public/static/js/wasm`
- `/wasm/web-ifc.wasm` 403
- 静态 JS 请求返回 HTML

### 8.2 固定修复方式

上传完成后在服务器执行：

```bash
cd /www/wwwroot/ioftv
bash server-permission-baseline.sh
```

### 8.3 目标权限状态

应满足：

- `PM2` 运行用户负责写：
  - `server/`
  - `server/uploads/`
  - `server/uploads/ifc/`
  - `server/uploads/ifc-cache/`
  - `storage/`

- `Nginx` 负责读：
  - `dist/`
  - `ifc/dist/`
  - `public/`

- `public/static/js/wasm/` 至少要：
  - Node 运行用户可写
  - `www-data` 可读

### 8.4 验证命令

```bash
pm2 status
ps -fp $(pm2 pid ioftv)
ls -ld /www/wwwroot/ioftv/server/uploads
ls -ld /www/wwwroot/ioftv/server/uploads/ifc
ls -ld /www/wwwroot/ioftv/public/static/js/wasm
```

## 9. 安装后端依赖

进入后端目录：

```bash
cd /www/wwwroot/ioftv/server
npm install --omit=dev
```

注意：当前项目服务器侧历史上出现过依赖声明不完整的问题。若 PM2 启动时报缺包，要优先检查：

- `compression`
- `pg`

如果部署环境里缺失，需要补装后再重启。

## 10. 清理旧 PM2 残留

先清理当前用户的历史进程：

```bash
pm2 delete ioftv 2>/dev/null || true
pm2 delete ioftv-node 2>/dev/null || true
```

如果发现：

- 杀掉 node 又自动起来
- `pm2 status` 和真实监听进程对不上

就要额外检查是否存在 `root` 侧 PM2 残留。

## 11. 启动后端

在项目根目录执行：

```bash
cd /www/wwwroot/ioftv
PORT=8890 \
DB_CLIENT=postgres \
PGHOST=localhost \
PGPORT=5432 \
PGDATABASE=postgres \
PGUSER=postgres \
PGPASSWORD=你的数据库密码 \
pm2 start server/index.js --name ioftv --update-env
```

保存 PM2：

```bash
pm2 save
```

## 12. 健康检查

```bash
pm2 status
curl http://127.0.0.1:8890/health
ss -lntp | grep 8890
```

正常应看到：

- `pm2 status` 中 `ioftv` 为 `online`
- `/health` 返回 `status: ok`
- 8890 由当前 Node 进程监听

## 13. Nginx 配置与重载

当前实际生效配置通常在：

```text
/etc/nginx/sites-available/default
/etc/nginx/sites-enabled/default
```

修改后执行：

```bash
sudo nginx -t
sudo nginx -s reload
```

## 14. 部署后必须验证的路径

至少要验证这些路径：

```bash
curl -I -H "Host: 192.168.60.10" http://127.0.0.1/
curl -I -H "Host: 192.168.60.10" http://127.0.0.1/ifc/
curl -I -H "Host: 192.168.60.10" http://127.0.0.1/wasm/web-ifc.wasm
curl -I -H "Host: 192.168.60.10" http://127.0.0.1/static/js/app.5599a9e2afa8316e842e.js
curl -I -H "Host: 192.168.60.10" http://127.0.0.1/api/projects
curl -I -H "Host: 192.168.60.10" "http://127.0.0.1/bigscreen/centermap?regionCode=china"
```

应分别验证：

- 首页返回 `200`
- IFC 查看器返回 `200`
- wasm 返回 `application/wasm`
- JS 返回 `application/javascript`
- API 返回 JSON

## 15. IFC 上传与同步链路验证

### 15.1 上传 IFC

页面上传时，如果报：

```text
EACCES: permission denied, open /www/wwwroot/ioftv/server/uploads/ifc/...
```

优先说明：

- `server/uploads/ifc` 不可被当前 Node 运行用户写入

第一时间执行：

```bash
cd /www/wwwroot/ioftv
bash server-permission-baseline.sh
pm2 restart ioftv --update-env
```

### 15.2 同步 IFC 构件

项目创建完成后，可直接测试：

```bash
curl -i -X POST \
  -H "Host: 192.168.60.10" \
  -H "x-user-role: admin" \
  http://127.0.0.1/api/projects/<项目ID>/components/sync-ifc
```

当前成功返回应类似：

```json
{"success":true,"added":231,"updated":0,"total":231}
```

### 15.3 当前已踩过的 IFC 问题

1. `compression` 未安装导致 PM2 启动失败
2. `pg` 未安装导致 PostgreSQL 路径不可用
3. `ifc-parse-worker.js` IPC 回传结果后过早退出，导致 `IFC 解析未完成`
4. `/wasm/web-ifc.wasm` 403
5. `server/uploads/ifc` 写权限错误导致上传 `EACCES`
6. 静态 JS 请求被错误回退为 HTML，浏览器报 `Unexpected token '<'`

## 16. 日志排障

### PM2 日志

```bash
pm2 logs ioftv --lines 100
```

注意：

- 旧日志里可能残留大量历史错误
- 如果想只看当前问题，先执行：

```bash
pm2 flush ioftv
pm2 logs ioftv --lines 0
```

再重新触发问题，只观察新输出。

### 典型旧日志误导

历史上曾大量残留：

```text
password authentication failed for user "postgres"
```

如果当前：

- `pm2 status` 正常
- `/health` 正常

那这些大概率只是旧日志，不是当前生效错误。

## 17. 一键清空脚本

如果需要彻底清空服务器上的项目目录与业务数据，可使用：

```bash
bash server-reset-clean.sh
```

这个脚本会：

- 删除 `/www/wwwroot/ioftv` 下应用文件
- 删除 PM2 中的 ioftv 进程
- 清空 PostgreSQL 业务表

执行前必须确认数据库账号密码配置正确。

## 18. 当前交接建议

接手人建议先读：

1. [README.md](./README.md)
2. [部署教程.md](./部署教程.md)
3. [AGENTS.md](./AGENTS.md)
4. [docs/project-context.md](./docs/project-context.md)

如果只看一份部署文档，就看这份，不再参考旧部署记录。
