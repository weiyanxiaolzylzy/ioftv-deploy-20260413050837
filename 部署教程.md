# IOFTV 宝塔迁移部署文档

> 适用场景：
> 把当前项目迁移到新的 Linux 服务器，使用宝塔面板 + Nginx + PM2 部署。
>
> 这份文档基于本次已经跑通的方案整理，目标是：
> 1. 你自己以后能照着操作
> 2. 任何 AI 拿到这份文档也能快速理解并继续部署

---

## 1. 最终部署架构

当前项目实际分成 2 部分：

1. 前端大屏
   - 目录：`dist/`
   - 作用：页面展示
   - 由 `Nginx` 直接托管

2. Node 后端
   - 目录：`server/`
   - 端口：`8890`
   - 作用：
     - `/api/*`
     - `/bigscreen/*`
   - 由 `PM2` 管理

### 最终流量关系

```text
浏览器
  -> Nginx(80)
     -> /                  -> /www/wwwroot/ioftv/dist
     -> /ifc/              -> /www/wwwroot/ioftv/ifc/dist
     -> /public/           -> /www/wwwroot/ioftv/public
     -> /uploads/          -> /www/wwwroot/ioftv/server/uploads
     -> /banzu/            -> /www/wwwroot/ioftv/banzu
     -> /wasm/             -> /www/wwwroot/ioftv/public/static/js/wasm
     -> /api/*             -> 127.0.0.1:8890
     -> /bigscreen/*       -> 127.0.0.1:8890
```

---

## 2. 服务器目录规范

建议统一使用这个目录：

```text
/www/wwwroot/ioftv/
```

最终目录结构：

```text
/www/wwwroot/ioftv/
├── dist/                       前端构建产物
├── ifc/
│   └── dist/                   IFC 查看器构建产物
├── server/                     Node 后端
│   ├── index.js
│   ├── package.json
│   ├── package-lock.json
│   ├── database.js
│   ├── data.json
│   └── uploads/
│       └── ifc/                上传的 IFC 文件
├── public/                     静态资源
│   ├── AlibabaPuHuiTi-3-45-Light/
│   ├── img/
│   ├── map-geojson/
│   └── static/js/wasm/
├── banzu/                      班组图片
└── 单个构件质检表/              Excel 模板
```

---

## 3. 本地打包原则

### 建议做法

前端和 IFC 在本地构建，服务器只负责运行。

本地执行：

```bash
npm run build
cd ifc
npm run build
```

### 上传时应包含

- `dist/`
- `ifc/dist/`
- `server/`（不带 `node_modules` 也可以）
- `public/`
- `banzu/`
- `单个构件质检表/`

### 上传时不要依赖服务器重新打前端

原因：

1. 服务器 Node 版本、依赖环境容易不一致
2. 前端构建失败后排查成本高
3. 本地构建可控，服务器只做发布

---

## 4. 本次实际成功的生产地址配置

前端生产环境要改成目标服务器地址。

`.env.production` 关键内容：

```env
NODE_ENV = production
VUE_APP_URL_ENV = production
VUE_APP_BACKEND_URL = http://81.70.191.16
VUE_APP_BASE_API = http://81.70.191.16/api
VUE_APP_IFC_URL = http://81.70.191.16/ifc
```

如果换新服务器，把 `81.70.191.16` 替换成新 IP 或域名即可。

### 注意

这里最终使用的是 `80` 端口对外访问，所以地址不再写 `:8080`。

---

## 5. 服务器软件要求

至少需要：

- Nginx
- Node.js
- npm
- PM2

示例安装：

```bash
apt update
apt install -y nginx build-essential
npm install -g pm2
```

建议 Node 版本：

- `20`
- 或 `24`

---

## 6. Node 后端部署

### 安装依赖

```bash
cd /www/wwwroot/ioftv/server
npm install --omit=dev
```

### 启动方式

不要用历史残留的 PM2 进程，统一只保留一个：

```bash
pm2 delete ioftv 2>/dev/null || true
pm2 delete ioftv-node 2>/dev/null || true

cd /www/wwwroot/ioftv/server
PORT=8890 pm2 start index.js --name ioftv
pm2 save
```

### 验证

```bash
pm2 status
curl http://127.0.0.1:8890/health
```

预期返回：

```json
{"status":"ok","service":"ioftv-api","port":8890}
```

---

## 7. Nginx / 宝塔站点配置

### 最终可用配置

站点配置文件示例：

```nginx
server {
    listen 80 default_server;
    server_name 81.70.191.16 _;

    client_max_body_size 500m;

    root /www/wwwroot/ioftv/dist;
    index index.html;

    location = /ifc {
        return 301 /ifc/;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8890;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /bigscreen/ {
        proxy_pass http://127.0.0.1:8890;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        alias /www/wwwroot/ioftv/server/uploads/;
        expires 7d;
        add_header Cache-Control "public";
    }

    location /banzu/ {
        alias /www/wwwroot/ioftv/banzu/;
        expires 7d;
        add_header Cache-Control "public";
    }

    location /public/ {
        alias /www/wwwroot/ioftv/public/;
        expires 30d;
        add_header Cache-Control "public";
    }

    location /wasm/ {
        alias /www/wwwroot/ioftv/public/static/js/wasm/;
        default_type application/wasm;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /ifc/ {
        alias /www/wwwroot/ioftv/ifc/dist/;
        try_files $uri $uri/ /ifc/index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 重载方式

宝塔环境里不要强依赖：

```bash
systemctl reload nginx
```

因为宝塔的 nginx 可能不是 systemd 管。

统一使用：

```bash
nginx -t
nginx -s reload
```

---

## 9. 验证清单

### 9.1 Node

```bash
curl http://127.0.0.1:8890/health
curl http://127.0.0.1:8890/bigscreen/centermap?regionCode=china
```

### 9.2 Nginx 静态资源

注意本机测试时最好带 Host 头，避免命中别的站点：

```bash
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/ifc/
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/map-geojson/china.json
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/static/js/app.xxx.js
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/public/AlibabaPuHuiTi-3-45-Light/AlibabaPuHuiTi-3-45-Light.woff2
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/uploads/ifc/你的文件.ifc
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/banzu/A4%E7%8F%AD%E7%BB%84.jpg
```

### 9.3 浏览器

最终访问：

```text
http://81.70.191.16/
http://81.70.191.16/ifc/
```

如果刚改完配置，浏览器要：

- `Ctrl + F5`
- 或开无痕窗口
- 或在开发者工具里勾选 `Disable cache`

---

## 10. 这次实际踩过的坑

下面这些都是真实踩过的坑，以后迁移时优先排查。

### 坑 1：`/bigscreen/centermap` 没代理，地图不显示

现象：

- 页面打开了
- 地图区域空白
- 浏览器里地图请求异常

原因：

- 地图组件调用的不是 `/api/...`
- 而是 `/bigscreen/centermap`
- 如果 Nginx 只代理 `/api/`，地图接口会返回错内容

解决：

- Nginx 必须加 `location /bigscreen/`

### 坑 2：`0.default.conf` 抢占 80 端口

现象：

- `curl http://127.0.0.1/` 返回 404
- `nginx -T` 提示：
  `conflicting server name "_" on 0.0.0.0:80`

原因：

- 宝塔默认站点 `0.default.conf` 占了 80
- 你的项目站点没有成为真正的默认站

解决：

- 停掉或改名：
  `/www/server/panel/vhost/nginx/0.default.conf`
- 项目站点改成：
  `listen 80 default_server;`

### 坑 3：字体 404

现象：

- 浏览器控制台报：
  - `AlibabaPuHuiTi-3-45-Light.woff2 404`
  - `AlibabaPuHuiTi-3-45-Light.woff 404`
  - `AlibabaPuHuiTi-3-45-Light.ttf 404`

原因：

- 页面引用的是 `/public/AlibabaPuHuiTi-3-45-Light/...`
- 但服务器没传 `public/` 或 Nginx 没映射 `/public/`

解决：

- 上传 `public/AlibabaPuHuiTi-3-45-Light/`
- Nginx 配好 `location /public/`

### 坑 4：IFC 文件 404

现象：

- IFC 页面能开
- 模型加载失败
- 控制台里 `/uploads/ifc/xxx.ifc 404`

原因：

- `server/uploads/ifc/` 没带上
- 或项目数据引用了不存在的 IFC 文件

解决：

- 确认文件真实存在：
  `/www/wwwroot/ioftv/server/uploads/ifc/`
- 确认 Nginx 配了 `/uploads/`

### 坑 5：`curl 127.0.0.1/...` 测试结果误导

现象：

- 服务器本机测 URL 返回 404
- 但带 Host 头后又正常

原因：

- Nginx 同时有多个站点
- 不带 Host 时可能命中错误的 `server`

正确测试方式：

```bash
curl -I -H "Host: 81.70.191.16" http://127.0.0.1/目标路径
```

---

## 11. 静态资源位置速查

### 背景图 / 面板图

本地：

- `public/背景.png`
- `public/panel.png`

### 首页/监控图片

本地：

- `public/img/welcome.jpg`
- `public/img/xiangji.jpg`
- `public/img/a4.jpg`

### 地图 geojson

本地：

- `public/map-geojson/china.json`
- `public/map-geojson/chinaNanhai.json`
- `public/map-geojson/*.json`

### 字体

本地：

- `public/AlibabaPuHuiTi-3-45-Light/`

### 班组图片

本地：

- `banzu/`

---

## 12. 以后迁移到新服务器的最简步骤

### 步骤 1：改前端生产地址并本地构建

改 `.env.production`：

- `VUE_APP_BACKEND_URL`
- `VUE_APP_BASE_API`
- `VUE_APP_IFC_URL`

然后：

```bash
npm run build
cd ifc
npm run build
```

### 步骤 2：上传整包到新服务器

上传到：

```text
/www/wwwroot/ioftv/
```

### 步骤 3：安装 Node 依赖

```bash
cd /www/wwwroot/ioftv/server
npm install --omit=dev
```

### 步骤 4：启动 Node

```bash
cd /www/wwwroot/ioftv/server
PORT=8890 pm2 start index.js --name ioftv
pm2 save
```

### 步骤 5：配置 Nginx

要点：

- `80` 端口
- `/api/`
- `/bigscreen/`
- `/ifc/`
- `/uploads/`
- `/public/`
- `/banzu/`
- `/wasm/`

### 步骤 6：清理冲突站点

优先检查：

```bash
grep -R -n "listen 80" /www/server/panel/vhost/nginx
grep -R -n "server_name _;" /www/server/panel/vhost/nginx
```

### 步骤 7：逐项验证

先测：

- Node health
- `/bigscreen/centermap`
- `/map-geojson/china.json`
- `/public/字体`
- `/uploads/ifc/...`

最后再开浏览器。

---

## 13. 运维常用命令

### Node

```bash
pm2 status
pm2 logs ioftv --lines 100
pm2 restart ioftv
pm2 delete ioftv
pm2 save
```

### Nginx

```bash
nginx -t
nginx -s reload
nginx -T
```

### 端口检查

```bash
ss -lntp | grep 80
ss -lntp | grep 8890
```

---

## 14. 最后的结论

这套项目迁移时，最关键的不是“代码能不能跑”，而是下面 5 件事必须同时成立：

1. 前端 `dist/` 和 `ifc/dist/` 上传完整
2. `public/`、`banzu/`、`uploads/` 这些静态资源目录上传完整
3. Nginx 不仅要代理 `/api/`，还必须代理 `/bigscreen/`
4. Node 用 PM2 管，避免混用其他手工启动方式
5. 宝塔默认站点和 `80` 端口冲突要先清掉

只要这 5 项都对，迁移就基本不会再出大问题。
