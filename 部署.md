# Docker 测试版部署说明

本文档用于把当前项目部署到服务器做测试版验收。

## 先说明清楚

本项目有两套工作方式，不能混用：

### 1. 本地开发

本地开发默认不用 Docker。

本地改代码、调试页面、调试接口时，使用：

```bash
npm run serve
npm run server:dev
```

也就是说：

- 前端单独跑
- 后端单独跑
- 本地开发默认不走 Docker

### 2. 服务器部署

只有在服务器上部署测试版或交付演示版时，才使用 Docker。

服务器部署的目标是：

- 用 Docker 跑应用服务
- 用服务器自己的 PostgreSQL 提供数据库

当前不是“应用容器 + PostgreSQL 容器”双容器方案。  
当前有效方案是：

- 1 个应用容器：`ioftv-app`
- 1 个服务器本机 PostgreSQL 服务

## 一、当前服务器部署结构

当前服务器部署结构如下：

- Docker 运行应用容器
- 应用监听端口：`8890`
- 数据库使用服务器本机 PostgreSQL
- Docker 容器连接宿主机 PostgreSQL

当前常用访问地址：

- 局域网：`http://192.168.60.10:8890`
- Tailscale：`http://100.104.212.6:8890`

说明：

- 局域网地址优先用于现场演示和验收
- Tailscale 地址可用于远程访问
- 如果 Tailscale 访问很慢，优先怀疑网络链路和大前端 chunk，不要先怀疑服务没启动

## 二、服务器需要具备的条件

服务器需要先安装：

1. Docker
2. Docker Compose Plugin
3. PostgreSQL

检查命令：

```bash
docker --version
docker compose version
psql --version
```

## 三、需要上传到服务器的内容

把整个项目目录上传到服务器，例如：

```bash
/opt/ioftv
```

至少应包含这些内容：

- `docker-compose.yml`
- `Dockerfile`
- `.dockerignore`
- `server/`
- `src/`
- `public/`
- `ifc/`
- `banzu/`
- `单个构件质检表/`
- `package.json`
- `package-lock.json`
- `.env.production`（如果当前部署需要）

## 四、数据库说明

当前部署使用服务器本机 PostgreSQL，不使用数据库容器。

### 1. 本地开发数据库

本地开发时，通常连接你本机 PostgreSQL。

### 2. 服务器数据库

服务器部署时，应用容器连接服务器自己的 PostgreSQL。

当前默认连接信息通常是：

- host: `127.0.0.1`
- port: `5432`
- database: `postgres`
- user: `postgres`

如果数据库已经提前导入项目数据，就不需要再次初始化空库。

## 五、第一次部署

进入服务器项目目录：

```bash
cd /opt/ioftv
```

执行：

```bash
sudo docker compose up -d --build
```

含义：

- `--build`：重新构建镜像
- `-d`：后台运行

## 六、查看是否启动成功

执行：

```bash
cd /opt/ioftv
sudo docker compose ps
```

正常情况应看到：

- `ioftv-app`

并且状态类似：

- `Up`
- `healthy`

再执行健康检查：

```bash
curl http://127.0.0.1:8890/health
```

如果返回类似：

```json
{"status":"ok","service":"ioftv-api","port":8890}
```

说明应用已经启动成功。

## 七、浏览器访问

### 1. 优先使用局域网地址

```text
http://192.168.60.10:8890
```

### 2. 远程访问可使用 Tailscale

```text
http://100.104.212.6:8890
```

### 3. 访问慢时的判断原则

如果：

- 服务器 `curl 127.0.0.1:8890` 正常
- 容器状态正常
- 远程浏览器仍然 `ChunkLoadError`

优先判断为：

- 远程链路慢
- 前端 chunk 太大
- Tailscale 路径不如局域网流畅

不是优先判断为“应用没起来”。

## 八、防火墙说明

服务器如果启用了 `ufw`，必须放行 `8890`：

```bash
sudo ufw allow 8890/tcp
sudo ufw reload
sudo ufw status
```

否则：

- 服务器本机可以访问
- 其他机器仍然访问不到

## 九、常用命令

### 1. 启动

```bash
cd /opt/ioftv
sudo docker compose up -d
```

### 2. 停止

```bash
cd /opt/ioftv
sudo docker compose down
```

### 3. 重启

```bash
cd /opt/ioftv
sudo docker compose restart
```

### 4. 重新构建并启动

```bash
cd /opt/ioftv
sudo docker compose up -d --build
```

### 5. 查看容器状态

```bash
cd /opt/ioftv
sudo docker compose ps
```

### 6. 查看应用日志

```bash
cd /opt/ioftv
sudo docker compose logs -f app
```

## 十、数据保存位置

当前部署通过挂载保留业务数据目录。

常见挂载目录包括：

- `./server/uploads`
- `./banzu`
- `./单个构件质检表`

这意味着：

- 上传的 IFC 文件不会因为容器重建立即丢失
- 班组照片不会因为容器重建立即丢失
- 质检模板不会因为容器重建立即丢失

数据库数据不保存在 Docker 数据库容器中，而是保存在服务器自己的 PostgreSQL 数据目录中。

## 十一、代码更新后如何部署

本地改完代码并同步到服务器后，执行：

```bash
cd /opt/ioftv
sudo docker compose up -d --build
```

即可更新服务。

## 十二、如果端口被占用

如果服务器上 `8890` 已被占用，可以修改 `docker-compose.yml` 中的端口映射。

例如把：

```yaml
ports:
  - "8890:8890"
```

改成：

```yaml
ports:
  - "8891:8890"
```

然后重新启动：

```bash
cd /opt/ioftv
sudo docker compose up -d --build
```

之后访问：

- `http://192.168.60.10:8891`
- `http://100.104.212.6:8891`

## 十三、最简单的一套部署操作

给测试版部署时，直接按这个顺序：

1. 上传整个项目到服务器 `/opt/ioftv`
2. 确认服务器 PostgreSQL 已安装并可用
3. SSH 登录服务器
4. 执行：

```bash
cd /opt/ioftv
sudo docker compose up -d --build
sudo docker compose ps
curl http://127.0.0.1:8890/health
```

5. 如果启用了防火墙，再执行：

```bash
sudo ufw allow 8890/tcp
sudo ufw reload
```

6. 浏览器优先打开：

```text
http://192.168.60.10:8890
```

## 十四、排查顺序

如果打不开，按这个顺序查：

### 1. 先查应用是否正常

```bash
curl -I http://127.0.0.1:8890
cd /opt/ioftv && sudo docker compose ps
cd /opt/ioftv && sudo docker compose logs --tail=200 app
```

### 2. 再查服务器内网地址是否可访问

```bash
curl -I http://192.168.60.10:8890
sudo ss -lntp | grep 8890
```

### 3. 再查防火墙

```bash
sudo ufw status
```

### 4. 再查远程链路问题

如果应用健康，但浏览器报：

- `ChunkLoadError`
- 大 JS 文件 timeout

优先判断：

- Tailscale 链路慢
- 前端大 chunk 加载超时

这时优先改用局域网地址测试。

## 十五、后续正式化方向

当前这套是测试版部署方案，目标是：

- 快速跑起来
- 方便验收和演示
- 不把本地开发流程和服务器部署流程混到一起

后续如果正式上线，再考虑：

- 独立 Nginx
- 更完整的数据库账号和权限管理
- 备份方案
- HTTPS
- 镜像仓库
- 前端 chunk 进一步拆分优化
