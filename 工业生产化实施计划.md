 # 工业生产化实施计划

## 目标

将当前系统从演示型 BIM + 质检平台，逐步升级为可用于真实工业生产的构件全生命周期管理系统。

核心要求：

- 保存所有构件主数据
- 保存每个构件的状态流转历史
- 支持班组、质检、照片、报告、任务、绩效的全链路关联
- 支持后续服务器部署、数据迁移、权限控制和审计追溯

## 总体方案

建议采用以下组合方案：

1. `PostgreSQL` 或 `MySQL` 作为主业务库
2. `MongoDB` 作为 IFC 原始属性和扩展 JSON 属性库
3. 图片、报告、IFC、点云文件使用对象存储或固定文件目录
4. 所有状态变化单独记录历史，不只保留当前状态

结论：

- `MongoDB` 可以使用，但不建议单独承担工业生产主库
- 更推荐“关系型主库 + 文档扩展库 + 文件独立存储”的混合架构

## 第一阶段：梳理主数据模型

先统一“构件”这条主线，确定每个构件的唯一身份。

需要明确：

- 一个构件的唯一键是什么
- `component_mark`、`globalId`、`expressID`、项目 ID 的关系
- 哪些字段属于主业务字段
- 哪些字段属于 IFC 扩展字段

输出物：

- 构件主数据字段清单
- 唯一标识规则
- 编号映射规则

## 第二阶段：设计数据库结构

将业务拆分为核心数据表。

建议至少包括：

- 项目表
- 构件主表
- 构件当前状态表
- 构件状态历史表
- 质检任务表
- 质检记录表
- 班组表
- 构件班组关联表
- 附件表
- 操作日志表

输出物：

- ER 图
- 表结构草案
- 索引与唯一约束设计

## 第三阶段：设计构件状态流转

将“构件状态”设计为标准流程，而不是只用一个文本字段。

建议参考流程：

1. 建模完成
2. 深化完成
3. 下料
4. 组立
5. 焊接
6. 喷涂
7. 待检测
8. 检测中
9. 检测合格
10. 检测不合格
11. 返修中
12. 入库
13. 发运
14. 到场
15. 安装完成
16. 归档

输出物：

- 状态机定义
- 状态流转规则
- 每个状态的责任角色与操作要求

## 第四阶段：设计文件与图片存储方案

照片、报告、IFC、点云文件不建议直接存数据库二进制。

建议方式：

- 数据库保存文件路径、文件 ID、上传人、上传时间、归属构件 ID
- 实际文件保存到固定目录、MinIO 或 OSS

输出物：

- 文件目录规范
- 数据库存储字段设计
- 上传和访问策略

## 第五阶段：设计 IFC 入库流程

将 IFC 导入、解析、入库流程标准化。

建议流程：

1. 上传 IFC
2. 解析构件清单
3. 提取 `component_mark`、`globalId`、`expressID`、类型、规格、属性
4. 写入构件主表
5. 原始 IFC 属性写入 `MongoDB` 或 `PostgreSQL JSONB`
6. 建立 assembly 与子构件映射关系

输出物：

- IFC 导入流程图
- IFC 字段映射表
- 异常处理规则

## 第六阶段：设计全链路追溯

确保每个构件都能追溯完整生命周期。

需要覆盖：

- 属于哪个项目
- 来自哪个 IFC 模型
- 分配给哪个班组
- 经过哪些状态
- 做过哪些质检
- 上传过哪些照片和报告
- 谁在什么时间做了什么操作

输出物：

- 追溯链模型
- 审计字段规范
- 操作日志规范

## 第七阶段：制定迁移路线

不建议一次性推翻现有系统，应采用分步迁移。

建议顺序：

1. 先补数据库设计
2. 再改构件主数据入库
3. 再改班组、照片、质检与构件的绑定方式
4. 再增加状态历史表
5. 最后改报表、绩效、追溯页面

输出物：

- 迁移阶段任务清单
- 风险点清单
- 回滚方案

## 第八阶段：上线前准备

真正投入工业生产前，还需补齐以下能力：

- 权限体系
- 数据备份
- 文件备份
- 操作审计
- 异常告警
- 数据校验
- 定时任务
- 部署规范

## 推荐结论

短结论如下：

- `MongoDB` 可以用，但更适合存 IFC 扩展属性
- 主业务库更建议使用 `PostgreSQL`
- 最优方案是“关系型主库 + 文档扩展 + 文件独立存储”

## 下一步

后续可在本计划基础上继续输出：

1. 适合当前项目的数据库表结构草案
2. 系统全链路业务流程图方案
3. 现有项目到生产版的平滑迁移路线图
