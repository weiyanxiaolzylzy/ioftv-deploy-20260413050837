
## 

## 第二阶段：同步修改页面显示

### 0. 检测总览 SVG 化改造

目标文件：

- 大屏首页检测总览所在组件
- 预计涉及首页总览卡片及其子项样式文件

修改目标：

- 按 SVG 方案重构“检测总览”圆环组件。
- 使用 `<svg>` 作为容器。
- 使用 `<circle>` 绘制背景环与进度环。
- 使用 `<text>` 绘制中心数字。
- 使用统一坐标系，确保缩放后数字始终稳定居中，不再出现数字与圆环重叠、偏移问题。

重构原则：

- 保留当前模块的大体布局位置，不大改外围容器结构。
- 优先替换圆环内部实现，不先动数据来源和业务逻辑。
- 样式上保持现有大屏视觉风格，不做额外花哨改版。

验收标准：

- 数字始终在圆心，不随分辨率和缩放偏移。
- 圆环与数字不重叠。
- 首页缩放、全屏、大屏比例变化时显示稳定。
- 不影响现有统计数据绑定。

### 1. 检测计划

目标文件：

- [src/views/indexs/left-bottom.vue](d:/Roaming/ioftv-deploy-20260413050837/src/views/indexs/left-bottom.vue:38)

修改目标：

- 只显示三项信息：
  - 项目名称
  - 构件编号
  - 班组
- 修复当前内容重合问题。
- 清理重复列定义，避免“构件编号”重复占位。

验收标准：

- 每条计划信息为三列稳定布局。
- 长文本可截断，不重叠。
- 不影响现有计划编辑和读取逻辑。

### 2. 当前检测信息

目标文件：

- [src/views/indexs/right-bottom.vue](d:/Roaming/ioftv-deploy-20260413050837/src/views/indexs/right-bottom.vue:101)

修改目标：

- 只显示三项信息：
  - 构件编号
  - 检测状态
  以及后续不合格项（只显示不合格项，先留空）
- 删除其余冗余信息显示。

验收标准：

- 面板信息层级更简洁。
- 编号和状态字段对齐，不挤压模型区域。
- 不影响当前 IFC 联动、选中和状态同步。

## 实施顺序

1
改“检测总览”SVG 仪表盘。
3. 再改“检测计划”布局。
4. 然后改“当前检测信息”字段精简。
5. 最后统一做一次联动回归。

## 备注

- 在第一阶段未完成前，不建议继续调这两个面板的文案字段，否则容易把“编号字段”和“名称字段”再次混用。
- 后续正式改动时，优先保留现有业务逻辑，只调整字段选择和布局结构。
